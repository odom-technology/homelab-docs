# AdGuard Home

Document redundant resolver deployment, configuration consistency, DHCP integration,
upstream selection, filtering policy, client behavior, monitoring, and emergency DNS
fallback. Never expose DNS publicly through router port forwarding.
