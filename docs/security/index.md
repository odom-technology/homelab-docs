# Security

Use least privilege, isolate public and CI workloads, protect management interfaces,
encrypt secrets, patch dependencies, retain useful logs, and test security controls.
Build management, server, public, IoT, sensor, and guest boundaries before placing
workloads on them; document any hardware limitation that prevents the intended policy.
