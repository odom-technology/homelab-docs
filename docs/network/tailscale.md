# Remote access

Remote-access policy should be designed after service identities and trust boundaries
are known. Prefer named hosts, service-oriented tags, least-privilege grants, and
Tailnet-only management endpoints. Never publish exact production policy unchanged.
