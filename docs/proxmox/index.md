# Proxmox

Use Proxmox hosts only for virtualization and host management. Place applications in
VMs or containers, reserve host resources, configure guest startup order, and document
node maintenance before the cluster carries important services.
