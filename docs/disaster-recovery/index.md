# Disaster recovery

The target is a documented rebuild from blank disks: regain credentials, restore safe
DNS, install Proxmox, recreate the cluster, provision guests, restore data, validate
services, and return clients to normal configuration. Test sections independently
before attempting a full exercise.
