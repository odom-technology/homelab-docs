# Sanitization

Remove credentials, tokens, account identifiers, Tailnet details, internal domains,
public IP addresses, MAC addresses, serial numbers, camera locations, personal names,
and exact production policy. Replace them with labeled examples and documentation
address ranges. Review rendered output and file history before publishing.
