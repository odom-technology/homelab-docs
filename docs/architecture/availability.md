# Availability

Document the behavior of every service when one node, two nodes, the switch, storage,
DNS, the router, or the entire rack is unavailable. Include explicit degraded modes and
manual recovery actions.
