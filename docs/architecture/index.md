# Architecture

Architecture documentation should show physical connectivity, guest placement, trust
boundaries, service dependencies, failure domains, storage ownership, and recovery
order. A cluster improves management and quorum but does not automatically provide
application high availability.
